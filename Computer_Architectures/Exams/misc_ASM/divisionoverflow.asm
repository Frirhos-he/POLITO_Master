.MODEL small
.STACK
.DATA

DIVIDEND DW 801
DIVISOR DB 2
RESULT DB 3 DUP(?)

.CODE
.STARTUP


XOR AX,AX
XOR BX,BX
XOR DX,DX

MOV AL,BYTE PTR DIVIDEND+1
MOV BL,BYTE PTR DIVISOR
DIV BL
MOV BYTE PTR RESULT+2,AL


MOV AL,BYTE PTR DIVIDEND
DIV BL
MOV BYTE PTR RESULT+1,AL 
MOV AL,0
DIV BL
MOV BYTE PTR RESULT,AL








;result on DX
XOR AX,AX; dividend
XOR BX,BX; divisor
XOR DX,DX; result

;assume no overflow

MOV AX,DIVIDEND
MOV BL,BYTE PTR DIVISOR
DIV BL
;result in 8 bit on al and remainder on ah
MOV BYTE PTR RESULT+1, AL
MOV AL,0
DIV BL
MOV BYTE PTR RESULT, AL


.EXIT